public class BlacksmithVillager extends Villager {
    
    // Constructor that takes a name parameter and calls the constructor of the superclass
    public BlacksmithVillager(String name) {
        // Call the superclass constructor with the name parameter and "Blacksmith" as the profession
        super(name, "Blacksmith");
    }
}
